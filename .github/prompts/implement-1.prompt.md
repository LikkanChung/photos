---
mode: agent
---
# Build
Implement Iteration 1 accoring to `impl-plan.md` and the provided context. The implementation should be done in the `frontend` folder of the repository.

## Instructions
- Keep a changelog in `changelog.md` in the root of the repository.
- Keep `impl-plan.md` up to date with the latest changes and update the iteration states with 'Completed' when done.
- At the end of each iteration, stop and ask me to test and confirm before moving on to the next iteration.